# AGENTS.md

Punto de entrada para agentes de IA (Claude Code, OpenCode, Kimi, Cursor, etc.).

**Lee [CLAUDE.md](CLAUDE.md)**: contiene propósito, stack, arquitectura resumida,
reglas críticas y el índice hacia `docs/`, `decisions/` y `experiments/`.

Este archivo es solo un alias — toda la información vive en `CLAUDE.md` para no
mantener dos copias que se desincronicen.
